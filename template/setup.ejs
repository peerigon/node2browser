window.Setup = function(modules) {
    var requiringModules = {},
        initializedModules = {};

    <%= getLookUpPaths %>
    <%= initModule %>
    <%= normalizePath %>
    <%= Require %>
    <%= Resolve %>

    initModule('<%= firstModule %>');
};

window.modules = {};
window.global = {};

(<%= modulesWrapperFunc %>)(window.modules, window.global);
window.Setup(window.modules);
delete window.Setup;
